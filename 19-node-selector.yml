apiVersion: v1
kind: Pod
metadata:
  name: nodeselector-sample
  labels:
    name: nodeselector-sample
spec:
  containers:
  - name: nginx
    image: nginx:latest
  nodeSelector:
    grouptype: default